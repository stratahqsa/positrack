# dependencies
node_modules/

# next.js
.next/

# snapshot data (published to the GitHub Release; local dev copy stays untracked)
data/

# typescript
*.tsbuildinfo
.vercel
# env files (never commit secrets) — but DO commit the example template
.env*
!.env.example
