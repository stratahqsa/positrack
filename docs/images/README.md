# Manual screenshots

[MANUAL.md](../MANUAL.md) references the images below. Each step also has a written
**“What you'll see”** description, so the manual is fully usable even before these are
added. Drop a PNG/JPG with the exact filename to have it render inline.

| File | What it should show |
|---|---|
| `chatgpt-1-settings.png` | ChatGPT → Settings → **Apps/Connectors** tab (with **Create app** + Developer mode) |
| `chatgpt-2-create-app.png` | The **New App** dialog: Name, Server URL `…/cmcp`, Auth = OAuth, checkbox, Create |
| `chatgpt-3-consent.png` | Posibolt **“Application Access Request”** consent page → **Allow Access** |
| `chatgpt-4-connected.png` | The Positrack connector **Connected** (URL `…/cmcp`, Authorization used: OAuth) |
| `chatgpt-5-query.png` | A chat answering a data question, leading with a chart |
| `claude-1-add.png` | Claude **Add custom connector** dialog (Name + Remote MCP server URL) |
| `claude-2-connected.png` | Claude Positrack connector connected, **“Other tools 24”** listing the `Yt …` tools |
| `claude-3-chart.png` | A Claude chat rendering a **bar chart** for a breakdown (e.g. PXB1 by state) |

> Note: these were captured live during setup verification, but the browser-automation
> tooling couldn't export the image files into the repo. Re-capture them by following
> the matching step in MANUAL.md (each step is exact), or screenshot during your own
> first install.
