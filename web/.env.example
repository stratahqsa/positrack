# POSX Control Tower — environment
# Copy to `.env.local` for local dev. NEVER commit real secrets.

# Shared access code for the login gate (server-side only).
# Locally this is `admin`. In production set a strong shared secret.
ACCESS_CODE=admin
